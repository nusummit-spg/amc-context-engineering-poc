# --- API framework (WS3) ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.8
pydantic-settings>=2.4

# --- LLM (shared client wrapper) ---
anthropic>=0.90

# --- Data stores ---
neo4j>=5.24
faiss-cpu>=1.8
fastembed>=0.3
sqlalchemy>=2.0

# --- AWS (prod: SSM secret load) ---
boto3>=1.34

# --- Content ingestion & parsing (WS2) ---
pymupdf>=1.24
python-docx>=1.1
python-pptx>=1.0
extract-msg>=0.48
openpyxl>=3.1
markdown-it-py>=3.0

# --- Context-engineering engine (merged: app/engine) ---
sentence-transformers>=3.0   # query embedding (paraphrase-multilingual-MiniLM)
gliner>=0.2                   # zero-shot NER (Layer B)
spacy>=3.7                    # NER Layer A (blank pipeline)
easyocr>=1.7                 # PDF image OCR (index-time only)
pillow>=10.0
python-dotenv>=1.0

# --- Utilities ---
httpx>=0.27
tenacity>=9.0

# --- SSL / Windows certificate store ---
truststore>=0.10


# --- Dev / test ---
pytest>=8.0
pytest-asyncio>=0.24
