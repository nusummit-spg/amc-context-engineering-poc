# Streamlit UI — self-sufficient: the local retrieval engine (faiss_store.py,
# llm_text_client.py, ner_pipeline.py, taxonomy_engine/, context_engine/) runs
# in-process, no separate FastAPI backend required.
streamlit
requests
pandas
neo4j
python-dotenv
boto3

anthropic>=0.90
faiss-cpu>=1.8
sentence-transformers>=3.0
gliner>=0.2
spacy>=3.7
pymupdf>=1.24
pillow>=10.0
