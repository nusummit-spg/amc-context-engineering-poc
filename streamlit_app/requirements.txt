# Thin client for QUERY (calls the FastAPI backend over HTTP — no torch,
# faiss, gliner, or sentence-transformers here; all retrieval runs in the
# API). The Analytics tab is the one exception: it reads Neo4j directly via
# graph_store.py for live graph browsing, hence the plain `neo4j` driver +
# python-dotenv (graph_store/config have no ML dependencies of their own).
streamlit
requests
pandas
neo4j
python-dotenv

# Local engine sandbox (context_engine/, evals/, the *_eval.py scripts, and
# faiss_store.py/llm_text_client.py/ner_pipeline.py themselves) — NOT needed
# by the deployed Streamlit UI (app.py/chat_view.py never import these), only
# for running the standalone retrieval engine copy directly, e.g.
# `python -m evals.judge_eval`. gliner/spacy are lazy-imported inside
# ner_pipeline.py (only needed once hybrid_graphrag's NER step actually runs).
anthropic>=0.90
faiss-cpu>=1.8
sentence-transformers>=3.0
gliner>=0.2
spacy>=3.7
